import { Relationship.Model } from './relationship.model';

describe('Relationship.Model', () => {
  it('should create an instance', () => {
    expect(new Relationship.Model()).toBeTruthy();
  });
});
